---
title: Upstream pass 4, VS Code 8e35945b to 832cf23c5
status: built
date: 2026-09-19
refs:
  - code://REFERENCE.md - the clone recipe and the revisions this pass started from
  - code://UPSTREAM.md - the running list this pass adds to, one box per item below
  - git://53fcb38 - the `do-check` skill, which is how this pass was made
---

# Upstream pass 4

VS Code `8e35945b` (2026-09-12) to `832cf23c5` (2026-09-19): 72 agentHost commits, 68 in the workbench chat tree, 230 under `src/vs/sessions`. **The wire did not move.** `git diff 8e35945b..832cf23c5 -- src/vs/platform/agentHost/common/state/protocol/` is empty and the vendored `.ahp-version` is still `fd0471d4`. The protocol repository itself moved one commit, `a21274d` to `8549827`, which adds a `CODEOWNERS` file and nothing else. Everything below is host or client implementation, and every item names the file that settles it and the file here it would change.

Paths are relative to the clone root at `832cf23c5`, or to this repository when prefixed `packages/` or `docs/`.

## The clone was missing the client

`src/vs/sessions` - the Sessions window, which is the reference client now - was not in the sparse checkout, and neither was `src/vs/workbench/services/agentHost`. 230 files changed under it in this range. A path outside the sparse set is not in the working tree at all: it lists nothing, and a search finds nothing in it without saying so, so the first client read of this pass reported on a client it could not see. Both paths are added, both `REFERENCE.md` files carry the fuller recipe, and the `do-check` skill states it as a rule.

## Taken

### The tools an agent is given

- **`add_artifact_or_reference` promotes a reference to an artifact in place, keeping the id.** A value already held as a reference is not a duplicate when the same value arrives as an artifact: it is promoted, and the id it already had is the id it keeps. `src/vs/platform/agentHost/common/sessionArtifactCollection.ts:164` (`addOrPromoteArtifact`), `src/vs/platform/agentHost/node/shared/artifactServerTools.ts:215-217`. Here `packages/sdk/src/artifacttools.ts:180-182` answers "Already recorded" and discards the upgrade, so a skill that re-adds a value as an artifact is handed a reference back.
- **Artifact tool answers are `<status>: <id>`.** `src/vs/platform/agentHost/node/shared/artifactServerTools.ts:216,236` answers `Added artifact` / `Added reference` / `Promoted artifact` / `Already recorded` / `Removed artifact` / `Removed reference` and then the id, where `describe(entry)` used to go. Here `packages/sdk/src/artifacttools.ts:184,189,215` still echoes id, type, label and value.
- **A pull request that is opened or reused is recorded as a session artifact.** `src/vs/platform/agentHost/node/agentHostPullRequestOperationProvider.ts:260-276`, both paths through `_finalize` at `src/vs/platform/agentHost/node/agentHostPullRequestOperationHandler.ts:451`; the artifact is `isArtifact: true`, labelled with the PR title and linked to its URL, and the URL is associated with the branch. Here `packages/sdk/src/changes.ts:565` (reused) and `:573` (opened) return a message and a follow-up only, and a grep for artifact writes in that file finds none. This one is a **re-opened tick**: `UPSTREAM.md:29` already says a pull request made by `prepare-pull-request` is one of these artifacts, and no code in the SDK writes one.
- **A response round that ends with no text and no tool calls is announced.** A `systemNotification` with empty content and `_meta.kind: 'responseRoundEnded'` settles an open thinking section. `src/vs/platform/agentHost/common/meta/agentSystemNotificationMeta.ts:25`, emitted at `src/vs/platform/agentHost/node/copilot/copilotAgentSession.ts:5241`, read by the reference client at `src/vs/workbench/contrib/chat/browser/agentSessions/agentHost/stateToProgressAdapter.ts:506`. Here `responseRoundEnded` appears nowhere under `packages/`, so a round that ends silently leaves the reasoning section open. What a Claude stream offers as the signal for it is Left open below.

### What the agent's own tools cost it

- **The tools named in the first-turn instruction are always loaded.** `src/vs/platform/agentHost/node/shared/artifactServerTools.ts:74,82,90` sets `deferLoading` per tool (add `false`, remove and list `true`), `src/vs/platform/agentHost/node/claude/claudeServerToolMcpServer.ts:76` passes `alwaysLoad: !def.deferLoading`, and `:252-259` appends a "List/remove (discover if needed)" sentence to both wordings. Here `alwaysLoad` appears nowhere under `packages/`: every `mcp__ahp__*` tool takes the SDK default, so the tool the instruction tells the model to call can be the one the model cannot see. The rule's direction is confirmed on the Codex side at `src/vs/platform/agentHost/node/codex/codexAgent.ts:2828-2830`, which keeps dynamic tools eager because Codex cannot report tool search.
- **`artifactToolsCompactPrompts`, a root config key, chooses the short artifact wording.** `src/vs/platform/agentHost/common/agentHostSchema.ts:562,903-909`, `src/vs/platform/agentHost/node/shared/artifactServerTools.ts:113,152-153,254-259`. The window carries a setting for it, `chat.artifactTools.compactPrompts`, mapped onto this root key at `src/vs/platform/agentHost/common/artifactToolsConfiguration.ts`, and the settings document the window writes is built from the host's own root config, so a key this host does not declare is a key nothing can set. Here `ROOT_CONFIG_SCHEMA` (`packages/sdk/src/host.ts:3358`) declares `defaultShell` and nothing else, and there is one wording.

### What a client is told about a session

- **A chat keeps the title it was given.** `src/vs/platform/agentHost/node/chatContributions/sessionTitle/sessionTitleContribution.ts:56,70,80-92` persists a per-chat custom title in the chat's own store, through `src/vs/platform/agentHost/node/localCommands/renameLocalCommand.ts:47-50` and `localChatCommand.ts:52`, and restores it when the chat hydrates. Here chats live in memory only (`packages/sdk/src/host.ts:2244`), so a renamed peer chat comes back unnamed after a restart; the write is at `packages/sdk/src/host.ts:3302`.
- **`deferredTitleGeneration`, and a `rename_chat` shaped by the session's strategy.** `src/vs/platform/agentHost/common/agentHostSchema.ts:553,885` declares the switch; under `utility` the tool is not offered, and under `deferred` it loses its `automatic` property and is re-described (`src/vs/platform/agentHost/node/shared/sessionServerTools.ts:1520-1531`). Here `rename_chat` is fixed: always offered, always carrying `automatic` (`packages/sdk/src/sessiontools.ts:497`).

### What the Claude backend reports as a customization

- **Plugins are top-level containers, their contributions stay out of the per-scope lists, and builtins are a container with real URIs.** `src/vs/platform/agentHost/node/claude/customizations/claudeSessionCustomizationDiscovery.ts:56-90,291-335,347-350` and `claudeBuiltinCommands.ts:119,162`. Here `customizationsOf` (`packages/agent-claude/src/session.ts`) synthesises three `directory` containers from the CLI's flat lists with invented `~/.claude/<kind>/<name>` URIs, and folds plugin-contributed entries and builtins into them. The attribution is reachable, but not from the call this host makes today: `initializationResult()`, which is what feeds `customizationsOf` at `packages/agent-claude/src/session.ts:1799` and `packages/agent-claude/src/probe.ts:62`, carries no plugin list, so the plugins have to be read from `reloadPlugins()` or from the message-stream init instead. The effect is what a window lists rather than what an agent can invoke.

## Already had

Read in the range and already true here; the reference line is what it was checked against.

- The AHP wire itself. Request params, actions, notifications and the session config keys are unchanged, so `packages/sdk/src/types/` and the `applyDispatch` table take nothing from this range.
- `_meta['vscode.removeSessionArtifact']` is advertised whenever removal is served, decoupled from the extension-method allow-list: `src/vs/platform/agentHost/node/protocolServerHandler.ts:688,1908`. Here `packages/sdk/src/host.ts:4103` advertises it and `:5409` serves it unconditionally.
- `deferLoading` never reaches the protocol: `src/vs/platform/agentHost/node/shared/agentServerToolHost.ts:277` strips it before publishing. Here the published `ToolDefinition` has no such field.
- Host tools are recognised under the Claude `mcp__<server>__<name>` prefix: `src/vs/platform/agentHost/common/serverToolNames.ts:37-39`. Here `packages/agent-claude/src/session.ts:625` already strips it and routes by name.
- The pull request behind a branch is associated with it in `_meta.github` after `create-pr`: `src/vs/platform/agentHost/node/agentHostPullRequestOperationProvider.ts:276`. Here `refreshPullRequests` already rewrites `pullRequestUrls` (`packages/sdk/src/host.ts:1964,2088,2108-2113`).
- Artifact tools' per-session enablement is now the plain one: `src/vs/platform/agentHost/node/shared/artifactServerTools.ts:157`. Here only the tools the embedder passed in are advertised (`packages/sdk/src/tools.ts:26`).
- Inbound token as `?tkn=` or `Authorization: Bearer`, refused before the upgrade: `src/vs/platform/agentHost/common/tunnelAgentHostConnector.ts:297`. Here `packages/sdk/src/listen.ts:36-44,79-80,95-97,195-198`.
- Host-to-client reverse requests: `src/vs/platform/agentHost/common/reconnectingTransport.ts:88-91`. Here `packages/sdk/src/rpc.ts:95-119` and `packages/sdk/src/host.ts:313,398`.
- Non-protocol `vscode/*` methods with `_meta` capability flags, and `-32601` for the rest: `src/vs/platform/agentHost/node/protocolServerHandler.ts:1822,2124`. Here `packages/sdk/src/host.ts:4103,5288-5460,6720`.

## Read and not taken

Kept so the next pass starts by reading it rather than re-deriving it.

- **Dev Containers as an extension surface.** `vscode/devContainers/{isDockerAvailable,connect,disconnect,relaySend}` and the `relayMessage`, `relayClose`, `closeConnection` and `output` notifications, `src/vs/platform/agentHost/node/devContainerAgentHostProtocol.ts:62-67,78-107`, gated by `initialize._meta['vscode.devContainers']` (`src/vs/platform/agentHost/common/meta/agentHostDevContainersMeta.ts:8`, gate at `src/vs/platform/agentHost/browser/agentHostProtocolClient.ts`, `get devContainerService()`). A host that omits the key is never asked. A product decision, not a defect; see Left open.
- **`setAgentMergeEnabled`, the only new server tool name in the range**, `{ enabled: boolean }`, `canRequireConfirmation`: `src/vs/platform/agentHost/node/shared/agentMergeServerTools.ts:12,19,152`. Agent Merge is a Copilot service; `packages/sdk/src/changes.ts:529` already says so when asked.
- **The central session catalog** (`agentHostCatalogListReader.ts`, `agentHostCatalogProjection.ts`, `agentHostCatalogReconciliationService.ts`, `agentHostCatalogSourceResolver.ts`, `agentHostCatalogSyncService.ts`, `agentHostSessionsV2MigrationService.ts`): none of them emits an action, a notification or a `_meta` key. They change how VS Code caches and repairs its own list, over the same `SessionSummary` fields.
- **The turn tracker and the telemetry around it** (`src/vs/platform/agentHost/node/agentHostTurnTracker.ts:96,333`, `agentSideEffects.ts:852-857,1967-2010`, `agentHostTelemetryReporter.ts`): turn ordinals, process age, send-stage durations, time to first substantive progress. Logged, never sent.
- **`_meta['vscode.modelCall']`** (`src/vs/platform/agentHost/common/meta/agentModelCallMeta.ts:9`): per-model-call diagnostics, stamped by the Copilot session only, with no in-tree renderer reading it.
- **`vscode.pullRequest` on an incoming chat message with title and description optional** (`src/vs/platform/agentHost/common/meta/agentPullRequestOperationMeta.ts:163`): an Agent Merge turn this host never receives.
- **Everything on the Copilot and Codex side**: BYOK model limits (`byokLmBridgeRegistry.ts:170-171`), Auto-model routing and its ungating, `read_agent`/`write_agent` display names, subagent quiet-period completion, `SKILL_CHAR_BUDGET`, Codex `thread/fork` bounds and `thread/revert`. A different SDK, with no AHP surface.
- **The terminal auto-approval rule engine** (`src/vs/platform/agentHost/node/commandAutoApprover.ts:53,86-87,644-648`, `src/vs/platform/agentHost/node/sessionPermissions.ts:558-576`): this host has no per-command approver and maps `autoApprove` and `mode` straight onto the Claude permission mode.
- **Everything on the transport's client side**: SSH `ProxyCommand` and known-hosts resolution (`sshProxyCommand.ts`, `sshConfigPaths.ts`, `sshConfigParsing.ts`), tunnel and relay diagnostics, `webSocketOverDuplex` framing limits, `logId` plumbing, `agentHostBootstrap` title callbacks. VS Code dials out; this daemon is dialed to.
- **The workbench window's own UI**: the working-progress logo, thinking and tool-chain CSS, theme colours, the edit-stats button, the archive nudge's layout, `editorChatUsage`, and the debug-log export's zip assembly. A host cannot feed any of it.

## Left open

- **Does this host want Dev Container sessions?** If no, omitting `_meta['vscode.devContainers']` is sufficient and nothing else is required. If yes, the work is the four methods, the four notifications, the capability key, and a reverse `vscode/requestWorkspaceTrust` before launching (`src/vs/platform/agentHost/node/devContainerAgentHostProtocol.ts:139-141`), with the relay carrying opaque base64 frames of the nested host's WebSocket.
- **The token in the announced URL.** `src/vs/platform/agentHost/node/agentHostServerMain.ts:342-348` prints its connect URLs with `?tkn=<token>`. Here `packages/server/src/main.ts:462-472` states the opposite rule in a comment: "Where the secret came from, never the secret: stdout is a log, and a log is the one place a credential should not end up." Settled by choosing which artifact may hold the secret; the 0600 daemon record (`packages/server/src/daemon.ts:133-144`) is the candidate.
- **Do artifact answers have to be byte-identical to the reference?** The reference reports ids and nothing else (`src/vs/platform/agentHost/node/shared/artifactServerTools.ts:216`). A skill or test asserting on the returned string settles it; otherwise promoting in place is the part that matters.
- **Does the reused-pull-request path record an artifact too?** The reference does both through one `_finalize` (`src/vs/platform/agentHost/node/agentHostPullRequestOperationHandler.ts:451`).
- **The window now tells a session's own pull requests from ones it inherited, and this host marks neither.** `src/vs/sessions/services/sessions/common/session.ts:400` (`getSessionOwnedGitHubPullRequestRefs`) counts a pull request as owned when it was recorded as a reference with a stable id, or when it is in `_meta.github.pullRequestUrls` but not in `initialPullRequestUrls`, or is listed in `associatedPullRequestUrls`. Those two keys are a pre-existing host convention, not part of this range: `src/vs/platform/agentHost/common/state/sessionState.ts:1682-1684`, set at `src/vs/platform/agentHost/node/agentHostGitStateService.ts:176-191` and promoted at `agentHostPullRequestOperationProvider.ts:276`, and none of those files changed since the floor. This host reports `pullRequestUrls` and no baseline (`packages/sdk/src/host.ts:2088`), so every pull request it reports reads as inherited, and the archive nudge's "all pull requests merged" test cannot come true against it. Settled by deciding whether this host keeps the baseline its `_meta.github` is missing.
- **Can the Claude backend see a round that ends empty**, which the `responseRoundEnded` item needs? `src/vs/platform/agentHost/node/copilot/copilotAgentSession.ts:5223` is the Copilot signal; whether the Claude stream exposes the same event is unread.
- **Does the window forward `artifactToolsCompactPrompts` and `deferredTitleGeneration` to a host whose root schema does not declare them?** The reference declares both (`src/vs/platform/agentHost/common/agentHostSchema.ts:553,562`); whether the keys are pushed blind or only where advertised decides whether these are declarations this host must add. A capture of a real window's `root/configChanged` settles it.
- **The detached-worktree comment is right, and it took a second look to see that.** `packages/sdk/src/host.ts:4095-4104` calls the detached worktree five "its dev container flow", and they are: the Dev Container sessions provider reads that capability and all three worktree methods before it will offer a container session (`src/vs/sessions/contrib/providers/agentHost/browser/devContainerAgentHostSessionsProvider.ts:224`, reading `src/vs/platform/agentHost/common/agentHostExtensionProtocol.ts:75`). An earlier read of this very pass called the comment stale, because the grep behind it ran while `src/vs/sessions` was still missing from the sparse set and so found only the definition and a test. That is the trap the skill names, sprung in the pass that named it, and it is why a negative search is re-run once a tree is added rather than trusted.
- **Pre-existing drift, re-confirmed and unrelated to this range.** `packages/server/src/main.ts` USAGE still says a directory outside `--path` "is refused", which `24bb04c` removed and `docs/DAEMON.md` contradicts; `docs/AGENT.md:122` lists `setModel` / `setPermissionMode` / `setEffort` / `setOutputStyle` as `Session` members where only `setConfig(key, value)` exists; `docs/DAEMON.md`'s options table omits `--sessions`, `--wire` and `--version`; `packages/sdk/src/host.ts:4546-4553` calls `resource*` read-only where the write half is served; `packages/sdk/src/host.ts:5144-5146` says `source` "is not served" where `fork` and `sideChat` are; and `packages/agent-claude/src/session.ts:2198` accepts six permission modes where `packages/agent-claude/src/claude.ts:130` advertises five.

## What the plan should be

The three items under "The tools an agent is given" are one unit: promote, the answer shape, and the pull-request artifact that needs promote to exist. The two Claude-backend items - eager tools and customization containers - are independent of them and of each other. Everything else is either a declaration to add once the forwarding question above is answered, or a decision recorded here rather than built.
